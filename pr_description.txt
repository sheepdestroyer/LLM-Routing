🎯 What: Added `test_sync_gemini_token.py` to test the missing edge case for token extraction in `sync_gemini_token.py` (line 60), specifically testing the scenario where parsing the expiry date fails.
📊 Coverage: Added a test covering the `Exception` catch block that parses a malformed ISO format `expiry` date and asserts the fallback expiration calculates correctly to 3600 seconds from now.
✨ Result: Successfully verified that passing a fake invalid string (like `this-is-not-a-valid-date`) results in the required warning output and the correct default expiry of +1 hour, improving safety and testing reliability.
