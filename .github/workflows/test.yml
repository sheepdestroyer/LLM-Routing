name: Run Tests

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install httpx==0.28.1 pytest pytest-asyncio anyio pyyaml fastapi uvicorn python-multipart asyncpg langfuse redis

      - name: Syntax Check
        run: |
          python3 -m compileall router/
          PYTHONPATH=.:router CONFIG_PATH=router/config.yaml python3 -c "import router.main"

      - name: Run Unit Tests
        run: CONFIG_PATH=router/config.yaml PYTHONPATH=.:router pytest --ignore=test_agy_behavior.py --ignore=test_agy_tiers.py

      - name: Run Breaker Verification
        run: python3 verify_breaker.py

      - name: Run Integration Verification
        run: python3 test_a2_verify.py
