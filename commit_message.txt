🧹 [code health] Remove unused variable in memory_mcp.py

🎯 What:
Removed the unused variable `res = r.json()` assignment in `router/memory_mcp.py:158` inside `handle_remember_memory`.

💡 Why:
This addresses a statically identified code health issue (Ruff unused variable warning). The variable `res` is never used, and checking `r.status_code == 200` is sufficient to establish a successful POST request. Its removal cleans up dead code.

✅ Verification:
Confirmed via `git diff` that only `res = r.json()` was removed, preserving `tag_str` and other functionality. Full test suite ran and all 335 tests passed successfully without regressions.

✨ Result:
Cleaner, more maintainable code with no unused variables in this function block, resolving static analysis warnings.
